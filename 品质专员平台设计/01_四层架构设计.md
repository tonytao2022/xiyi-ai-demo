  deliverables    TEXT COMMENT '交付物说明',
  remark          TEXT COMMENT '备注',
  created_by      VARCHAR(50),
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (plan_id) REFERENCES ap_capa_plan(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='CAPA任务分解';

-- ============================================================
-- CAPA任务跟踪记录
-- ============================================================
CREATE TABLE ap_capa_task_track (
  id              BIGINT PRIMARY KEY AUTO_INCREMENT,
  task_id         INT NOT NULL COMMENT '任务ID',
  track_time      DATETIME NOT NULL COMMENT '跟踪时间',
  track_type      ENUM('status_update','progress','issue','verification','comment') NOT NULL COMMENT '跟踪类型',
  content         TEXT NOT NULL COMMENT '跟踪内容',
  attachment_url  VARCHAR(500) COMMENT '附件链接',
  verifier        VARCHAR(100) COMMENT '验证人',
  verify_result   ENUM('pass','fail','partial') COMMENT '验证结果',
  created_by      VARCHAR(50),
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (task_id) REFERENCES ap_capa_task(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='CAPA任务跟踪记录';

-- ============================================================
-- 场景报告模板：各场景的输出报告格式定义
-- ============================================================
CREATE TABLE ap_scene_report_tpl (
  id              INT PRIMARY KEY AUTO_INCREMENT,
  tpl_code        VARCHAR(50) NOT NULL UNIQUE COMMENT '模板编码',
  tpl_name        VARCHAR(200) NOT NULL COMMENT '模板名称',
  scene_id        INT NOT NULL COMMENT '关联场景ID',
  report_type     ENUM('markdown','html','json','pdf') DEFAULT 'markdown' COMMENT '报告格式',
  tpl_content     TEXT NOT NULL COMMENT '模板内容(占位符: {{variable}})',
  sections        JSON COMMENT '报告章节定义(title,order,fields)',
  tpl_vars        JSON COMMENT '模板变量定义列表',
  sort_order      INT DEFAULT 0,
  is_default      TINYINT(1) DEFAULT 0 COMMENT '是否默认模板',
  status          ENUM('draft','published','deprecated') DEFAULT 'published',
  created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (scene_id) REFERENCES ap_scene_config(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='报告模板定义';
```

### 2.4 七步分析流程结构化存储示意

以QUAL_01（问题定义与数据采集）为例，配置数据如下：

```json
// ap_scene_step 配置数据样例
{
  "scene_id": 1,  // QUAL_01
  "steps": [
    {
      "step_code": "QUAL_01_STEP_01",
      "step_name": "问题现象描述",
      "step_type": "definition",
      "sort_order": 1,
      "description": "定义质量问题的具体现象表现",
      "input_desc": "现场质检记录、异常报告、客户投诉信息",
      "output_desc": "结构化的问题现象描述(5W1H格式)",
      "analysis_model_id": 101,  // LLM推理模型
      "is_ai_required": 1,
      "is_manual_input": 1       // 需要人工输入原始问题
    },
    {
      "step_code": "QUAL_01_STEP_02",
      "step_name": "数据采集配置",
      "step_type": "definition",
      "sort_order": 2,
      "description": "配置问题相关的数据采集范围和参数",
      "input_desc": "数据源绑定配置、时间范围、筛选条件",
      "output_desc": "SQL查询参数/数据拉取任务定义",
      "analysis_model_id": null, // 不需要AI
      "is_ai_required": 0,
      "is_manual_input": 0
    }
  ]
}
```

---

## 三、场景配置数据模型详解

### 3.1 一个完整场景需要配置的信息

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                         场景配置数据模型                                      │
│                                                                              │
│  [场景基本信息]                                                              │
│  ├─ scene_code: 唯一编码(QUAL_01~QUAL_07)                                   │
│  ├─ scene_name: 场景名称                                                     │
│  ├─ scene_desc: 业务背景描述                                                  │
│  ├─ role_type: 角色类型(quality)                                              │
│  └─ tags: 标签(用于搜索和分类)                                                │
│                                                                              │
│  [步骤定义]  ═══ 1:N                                                         │
│  ├─ 步骤顺序(1-7)                                                            │
│  ├─ 步骤类型(definition/analysis/correlation/verification/attribution/       │
│  │           solution/tracking)                                              │
│  ├─ 输入输出说明                                                             │
│  ├─ 绑定的分析模型(ap_analysis_model)                                        │
│  └─ 绑定的报告模板(ap_scene_report_tpl)                                      │
│                                                                              │
│  [数据源绑定] ═══ 场景→步骤→指标/字段 → 治理层 → 采集层                      │
│  ├─ 通过scene_id + 可选的step_id关联                                        │
│  ├─ 绑定指标(indicator_code)                                                 │
│  ├─ 绑定标准字段(std_column_code)                                            │
│  ├─ 绑定实际数据表(table_source)                                             │
│  └─ 过滤条件(filter_condition)                                               │
│                                                                              │
│  [分析模型] ═══ 步骤绑定                                                     │
│  ├─ 模型编码/名称                                                            │
│  ├─ 类型(llm_reasoning/rule_engine/statistical...)                           │
│  ├─ 引擎配置(LLM的prompt/规则引擎的规则集)                                   │
│  ├─ 输入Schema(参数定义)                                                     │
│  └─ 输出Schema(结果格式)                                                    │
│                                                                              │
│  [报告模板] ═══ 步骤绑定                                                     │
│  ├─ 模板内容(含变量占位符)                                                   │
│  ├─ 章节定义(标题/顺序/字段)                                                │
│  └─ 变量定义(变量编码/名称/数据来源)                                        │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 场景配置的SQL存储关系

```
ap_scene_config (场景表)
    │ 1
    ├─── N ──── ap_scene_step (步骤表)
    │               │ 1
    │               ├─── N ──── ap_analysis_model (分析模型)
    │               │               │ 1
    │               │               └─── N ──── ap_model_input_param (模型参数)
    │               │
    │               └─── N ──── ap_scene_report_tpl (报告模板)
    │
    ├─── N ──── ap_scene_ds_binding (数据源绑定)
    │               │
    │               └─── 引用治理层: dg_indicator_atom / dg_standard_column
    │                              └─── 引用采集层: ds_table_metadata / ds_column_metadata
    │
    └─── N ──── ap_analysis_instance (分析实例 - 运行时数据)
                    │
                    └─── N ──── ap_analysis_step_log (步骤执行日志)
                                    │
                                    └─── N ──── ap_capa_plan (CAPA方案)
                                                    │
                                                    └─── N ──── ap_capa_task (CAPA任务)
                                                                    │
                                                                    └─── N ──── ap_capa_task_track (跟踪记录)
```

---

## 四、四层架构模块划分与管理策略

### 4.1 各层模块职责

| 层级 | 子模块 | 核心能力 | 依赖关系 |
|------|--------|---------|---------|
| **展现层** | quality-workbench | 品质专员SPA工作台 | 依赖应用层API |
| | config-console | 场景/数据源/指标配置管理 | 依赖治理层API |
| | report-viewer | 报告查看/导出/打印 | 依赖应用层API |
| | dashboard | 品质KPI总览看板 | 依赖治理层指标 |
| **应用层** | scene-engine | 场景注册/步骤编排/流程驱动 | 依赖治理层 |
| | analysis-pipeline | 七步分析法流水线引擎 | 依赖模型/规则 |
| | capa-manager | CAPA生命周期管理 | 独立模块 |
| | model-inference | AI推理/规则计算 | 依赖OpenClaw LLM |
| **治理层** | metadata-center | 元数据注册/血缘/版本管理 | 依赖采集层 |
| | indicator-engine | 指标定义/计算/聚合/预警 | 独立模块 |
| | standard-mgmt | 数据标准/编码/格式管理 | 独立模块 |
| | quality-audit | 数据质量规则/稽核 | 依赖采集层 |
| **采集层** | connector-mgmt | 数据源连接/连接池/心跳 | 外部系统 |
| | sync-engine | 增量/全量/实时同步 | 依赖connector |
| | import-service | 文件导入/解析/校验 | 依赖connector |
| | bridge | 外部API桥接 | 依赖connector |

### 4.2 各层技术栈建议

| 层级 | 技术选型 | 理由 |
|------|---------|------|
| **展现层** | 原生HTML/CSS/JS (深色工业风) | 复用stock-manager框架, 保持风格统一 |
| **应用层API** | Express.js (已有Node环境) | 端口8890, 延续8887/8888/8889序列 |
| **治理层API** | Express.js (与应用层共用) | 前后端分离, 或统一在8890端口 |
| **数据层** | MySQL (已有) | 无需新数据库, 使用xiyi_quality DB |
| **AI推理** | OpenClaw Agent + LLM | 利用deepseek/deepseek-v4-flash |
| **规则引擎** | 自研轻量规则引擎 | JSON配置规则+JavaScript执行器 |
| **定时任务** | OpenClaw cron | 复用现有调度能力 |

---

## 五、6个品质场景数据源映射

基于《智能体应用需求表-品质》的7个场景，数据源映射关系如下：

### 5.1 各场景涉及的业务表和字段

| 场景 | 涉及业务系统 | 核心数据表 | 关键字段 |
|------|-----------|-----------|---------|
| QUAL_01 问题定义 | MES质检系统 | quality_inspections | item_name, spec_min, spec_max, actual_value, result, defect_reason |
| | 异常管理系统 | anomaly_events | event_type, severity, title, description |
| | 客诉系统 | customer_complaints | complaint_type, product_code, defect_desc |
| QUAL_02 现象分析 | MES质检系统 | quality_inspections | inspect_time, item_name, result, trend数据 |
| | 生产工单 | work_orders | wo_no, product_code, scrap_qty, rework_qty |
| QUAL_03 4M1E分析 | 设备管理系统 | machine_equipment | machine_id, status, maintenance_record |
| | 人员档案 | employee_info | emp_id, skill_level, training_record |
| | 物料批次 | material_batch | batch_no, supplier, spec, inspection_result |
| | 工艺标准 | process_standard | process_code, standard_params, tolerance |
| | 环境监测 | environment_monitor | temp, humidity, dust, time |
| | 测量设备 | measure_device | device_id, calibration_date, accuracy |
| QUAL_04 根因验证 | 所有以上表 | 关联查询驱动 | 跨表数据交叉验证 |
| QUAL_05 能力归因 | 培训记录 | training_records | emp_id, course, score, date |
| | 技能矩阵 | skill_matrix | emp_id, skill_code, level, certify_date |
| | 绩效考核 | performance | emp_id, period, quality_score, efficiency |
| QUAL_06 CAPA方案 | 方案管理 | capa_plan + capa_task | (系统内数据) |
| QUAL_07 任务跟踪 | 任务管理 | capa_task + capa_task_track | (系统内数据) |

### 5.2 字段到标准模型的映射策略

每个"品质场景涉及的业务字段"通过以下链路映射到系统的标准数据模型：

```
原始业务字段（如MES的qc_result）  
        │
        ▼
ds_column_metadata (数据采集层注册)
  - source_column: qc_result
  - data_type: varchar(10)
  - enum_values: ["PASS","FAIL","REWORK"]
        │ ds_mapping_rule
        ▼
dg_standard_column (数据治理层标准字段)
  - std_code: QUAL_INSPECT_RESULT
  - std_name: 检验结果
  - enum_dict: INSPECT_RESULT_DICT (PASS=合格/FAIL=不合格/REWORK=返工)
        │ ap_scene_ds_binding
        ▼
ap_model_input_param (模型输入参数绑定)
  - param_code: inspect_result
  - data_source: QUAL_INSPECT_RESULT
```

---

## 六、企微及stock-manager前端风格统一方案

### 6.1 当前风格对比

| 维度 | stock-manager (深色工业风) | 品质专员工作台v6 DEMO (浅色商务风) | 天管品质专员原型 (深蓝专业风) |
|------|---------------------------|-----------------------------------|---------------------------|
| 背景色 | `#0a0e17` 深黑 | `#f5f7fa` 浅灰 | `#f8f9fa` 浅灰 |
| 卡片 | `#161d2a` 深色卡片 | `#ffffff` 白色卡片 | `#ffffff` 白色卡片 |
| 文本 | `#e2e8f0` 亮色 | `#212529` 深色 | `#333333` 深色 |
| 主题色 | `#3b82f6` (蓝色) | `#0d6efd` (蓝色) | `#1a237e` (深蓝) |
| 侧栏 | `#0d1321` 深色 | `#ffffff` 白色 | `linear-gradient(180deg, #1a237e, #283593)` |
| 整体感觉 | 证券交易系统风格 | SaaS企业管理后台 | 企业级工业系统 |

### 6.2 统一方案建议

针对"兮易AI大脑 - 品质专员"这个Demo，建议采用**深色工业风的制造业变体**，既复用stock-manager的框架和CSS变量体系，又加入制造业品质场景的色彩语言：

**配色方案调整**：
```css
:root {
  /* 复用stock-manager基础变量 */
  --bg-primary: #0a0e17;
  --bg-secondary: #111827;
  --bg-card: #161d2a;
  --text-primary: #e2e8f0;
  
  /* 新增品质场景特有色彩 */
  --color-quality: #10b981;     /* 质量绿 */
  --color-defect: #ef4444;     /* 缺陷红 */
  --color-warning: #f59e0b;   /* 预警黄 */
  --color-inspect: #3b82f6;   /* 检验蓝 */
  --color-trace: #8b5cf6;     /* 追溯紫 */
  --color-4m1e: #06b6d4;     /* 4M1E青 */
}
```

**前端模块规划**：
| 模块 | 实现方式 | 说明 |
|------|---------|------|
| 品质工作台SPA | 新增`quality-workbench.html` | 独立SPA，单独端口(8890)，保持与stock-manager物理隔离 |
| CSS复用 | 提取共同CSS变量文件 | `common-vars.css` 共享深色工业风变量 |
| 导航风格 | 使用统一侧栏+面包屑 | 品质场景的7步导航融入整体框架 |
| 图表库 | ECharts（与stock-manager一致） | 复用echarts渲染 |

---

## 七、管理配置界面设计

### 7.1 配置菜单结构

```
系统管理配置台
├── 📋 场景配置
│   ├── 场景列表 (CRUD场景)
│   ├── 步骤编排 (拖拽排序)
│   ├── 数据源绑定 (场景→数据源关联)
│   └── 报告模板 (模板编辑)
├── 🔗 数据源管理
│   ├── 数据源注册 (添加/测试连接)
│   ├── 表结构管理 (导入表结构)
│   ├── 字段映射 (源→标准字段映射)
│   └── 同步配置 (调度策略/增量方式)
├── 📊 指标管理
│   ├── 指标体系 (分类树)
│   ├── 原子指标 (定义/口径/阈值)
│   ├── 派生指标 (公式编辑器)
│   └── 指标看板 (预览验证)
├── 📐 数据标准
│   ├── 标准字典 (枚举/编码管理)
│   ├── 标准字段 (标准字段定义)
│   └── 数据格式 (格式规范管理)
├── ✅ 质量规则
│   ├── 规则定义 (SQL/脚本编辑)
│   ├── 调度配置 (执行周期)
│   ├── 稽核日志 (执行记录)
│   └── 质量报告 (统计分析)
└── 🤖 分析模型
    ├── 模型注册 (编码/类型/引擎)
    ├── 参数配置 (输入输出Schema)
    ├── 版本管理 (版本控制)
    └── 测试验证 (在线调试)
```

### 7.2 配置页面清单

| 页面 | 核心功能 | 复杂程度 |
|------|---------|---------|
| scene-list | 场景CRUD、状态管理、版本控制 | 中 |
| step-editor | 步骤添加/编辑/删除、拖拽排序、模型绑定 | 高 |
| ds-binding | 场景-步骤→治理层指标→实际数据源的可视化绑定 | 高 |
| report-tpl-editor | 模板内容编辑(带变量插槽)、章节配置 | 中 |
| data-source-reg | 数据源连接配置、测试连接 | 低 |
| table-import | 从数据源导入表结构、自动扫描 | 中 |
| field-mapping | 源字段→标准字段拖拽映射、转换规则 | 高 |
| indicator-tree | 指标体系分类树(递归展示) | 中 |
| atom-indicator-editor | 指标定义、口径编辑、阈值配置 | 中 |
| derived-editor | 公式编辑器(引用原子指标、表达式校验) | 高 |
| dict-manager | 字典条目CRUD、树结构管理 | 中 |
| rule-editor | 规则SQL编辑器、语法校验 | 高 |
| quality-dashboard | 稽核通过率展示、趋势图表 | 中 |

---

## 八、预估工作量与实施路径

### 8.1 总体工作量

| 模块 | 文件/组件数 | 预估代码行数 | 预估人天 | 难度 |
|------|-----------|------------|---------|------|
| **数据采集层** | 6个模块 | ~800行 | 1.5天 | 中 |
| **数据治理层** | 8个模块 | ~1200行 | 2天 | 高 |
| **数据应用层** | 7个模块 | ~2000行 | 3天 | 高 |
| **数据展现层 - 品质工作台** | 7个页面 | ~1800行 | 2.5天 | 中 |
| **管理配置界面** | 12个页面 | ~2500行 | 3.5天 | 高 |
| **集成联调** | - | - | 1.5天 | 中 |
| **总计** | ~40个 | ~8300行 | **14天** | - |

### 8.2 分角色工作量

| 角色 | 负责范围 | 预估人天 |
|------|---------|---------|
| **Antony (产品架构师)** | 场景配置引擎、七步分析流水线、CAPA管理、模型注册 | 4天 |
| **Hugo (数据工程师)** | 采集层管道、治理层元数据、指标引擎、质量规则 | 4天 |
| **Tony (架构师)** | 品质工作台、管理配置界面、风格统一、联调部署 | 6天 |

### 8.3 建议分阶段实施路径

#### Phase 1 (Week 1-2): 核心骨架 + 展现优先
```
目标: 可演示的MVP，展示1-2个完整场景(Demo用)

Day 1-2 (Hugo)
  - 建表(全部DDL)
  - 实现数据源注册+字段映射(px1场景)
  - 生成模拟质检数据

Day 1-3 (Tony)
  - 搭建品质工作台SPA框架(深色工业风)
  - 实现7场景导航菜单
  - 1个完整场景的前端页面

Day 3-4 (Antony)
  - 场景配置API(CRUD)
  - 七步分析流水线基础(步骤驱动)
  - LLM推理集成

Day 5: 集成联调、Demo演示准备
```

#### Phase 2 (Week 3-4): 完整能力
```
目标: 7个场景全部跑通

Week 3:
  - Hugo: 完整的数据治理+质量规则
  - Tony: 完整7场景工作台+1-2个配置页面
  - Antony: CAPA管理+任务跟踪

Week 4:
  - 管理配置台全部页面
  - 场景配置拖拽编辑器
  - 数据验证测试
  - 文档+演示准备
```

#### Phase 3 (Week 5-6): 可配置平台化
```
目标: 可配置平台，支持新增场景角色

- 多角色支持(设备专员、生产专员...)
- 场景配置可视化编辑器
- 指标计算公式编辑器
- 报告模板在线编辑
- 权限管理
- 部署文档+操作手册
```

### 8.4 交付成果清单

| 交付物 | 说明 | 责任人 |
|-------|------|-------|
| 数据库初始化SQL | 全部27张表DDL | Hugo |
| 数据生成脚本 | 模拟质检数据(7场景) | Hugo |
| 后端API服务 | Express.js应用(8890端口) | Antony |
| 品质工作台SPA | 深色工业风前端(7场景+报告) | Tony |
| 管理配置台 | 场景/数据源/指标/规则配置 | Tony |
| 分析模型配置 | LLM Prompt/规则引擎配置 | Antony |
| 架构文档 | 本设计文档 | 联合产出 |
| API文档 | 接口规范+请求/响应示例 | Antony |
| 演示脚本 | Storyline + 操作流程 | 联合产出 |
