---
description: 项目级 AI 应用通用约定
alwaysApply: true
---

# 项目总则

- 这是一个制造咨询 AI 应用，不是通用聊天机器人项目。
- 优先保证可信数字和可审计性，不要让模型决定指标口径。
- 前端只渲染 `StructuredReport`，不要在前端新增业务判断逻辑。
- OpenClaw 只做编排、模型路由和 Prompt 装配，不直接访问数据库。
- 涉及架构、场景、实施计划时，优先更新 `docs/architecture/` 下对应分类文档。
- 新增文档优先使用中文文件名，并放入合适的分组目录。
