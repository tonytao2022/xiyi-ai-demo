# 复制为仓库根目录 `.env`。`pnpm --filter bff dev` 时工作目录在 apps/bff，BFF 已改为从 monorepo 根加载此处配置。
DB_HOST=361361.cn
DB_PORT=22506
DB_USER=root
DB_PASSWORD="wyZ!(93313"
MES_DB_NAME=mes_demo
APP_DB_NAME=ce_agent_demo

SEMANTIC_API_BASE_URL=http://localhost:3010
PLAYBOOK_ENGINE_BASE_URL=http://localhost:3020
REPORT_SERVICE_BASE_URL=http://localhost:3030
OPENCLAW_RUNTIME_BASE_URL=http://localhost:3040
# 可选：外部 OpenClaw 执行地址。配置后，BFF 将优先调用该地址
# 单一 Gateway 入口时：所有 skillKey 共用此 URL，请求体仍会带 skillKey
# 例如：https://openclaw.example.com/api/v1/skills/quality_first_pass_yield/execute
OPENCLAW_EXECUTE_URL=http://127.0.0.1:18789/v1/responses
# 可选：仅 Trend Brief 技能单独指向另一执行端（不设则用 OPENCLAW_EXECUTE_URL 或默认 /skills/:skillKey/execute）
# OPENCLAW_EXECUTE_URL_TREND_BRIEF=
# 可选：外部 OpenClaw 的 Bearer Token（若平台要求鉴权）
OPENCLAW_AUTH_TOKEN=0283269a8fb92070ef5a36701d4887d564577f3d28a812eb
# 可选：调用 OpenClaw 超时（毫秒）
OPENCLAW_REQUEST_TIMEOUT_MS=30000
# 可选：外部 OpenClaw responses/chat_completions 总结超时
OPENCLAW_COMPAT_TIMEOUT_MS=30000
# 可选：本地/skills/:skillKey/execute 运行时超时
OPENCLAW_RUNTIME_TIMEOUT_MS=30000
# 可选：OpenClaw 请求重试次数与间隔
OPENCLAW_MAX_ATTEMPTS=2
OPENCLAW_RETRY_DELAY_MS=1200
# 可选：默认编排器（openclaw | hermes）
ORCHESTRATOR_DEFAULT_MODE=openclaw
# 可选：Hermes 执行地址与鉴权（启用 orchestratorMode=hermes 时生效）
# HERMES_EXECUTE_URL=http://127.0.0.1:8642/v1/responses
# HERMES_AUTH_TOKEN=
# 可选：Hermes 请求超时与重试
HERMES_REQUEST_TIMEOUT_MS=30000
HERMES_MAX_ATTEMPTS=2
HERMES_RETRY_DELAY_MS=1200
# 可选：OpenClaw 会话策略。生产分析任务建议保持 task_isolated，仅多轮追问再评估 follow_up_session
OPENCLAW_SESSION_POLICY=task_isolated
# 可选：任务总执行预算（不含排队）
TASK_EXECUTION_TIMEOUT_MS=120000
# 可选：任务队列与 worker 并发保护
TASK_QUEUE_MAX_SIZE=100
TASK_WORKER_MAX_CONCURRENT=2
TASK_WORKER_MAX_CONCURRENT_PER_TENANT=1
# 可选：下游服务并发保护
OPENCLAW_MAX_CONCURRENT=2
OPENCLAW_MAX_CONCURRENT_PER_TENANT=1
SEMANTIC_API_MAX_CONCURRENT=6
SEMANTIC_API_MAX_CONCURRENT_PER_TENANT=3
PLAYBOOK_ENGINE_MAX_CONCURRENT=4
PLAYBOOK_ENGINE_MAX_CONCURRENT_PER_TENANT=2
REPORT_SERVICE_MAX_CONCURRENT=4
REPORT_SERVICE_MAX_CONCURRENT_PER_TENANT=2
# 可选：下游服务分层超时
SEMANTIC_API_REQUEST_TIMEOUT_MS=12000
PLAYBOOK_ENGINE_REQUEST_TIMEOUT_MS=12000
REPORT_SERVICE_REQUEST_TIMEOUT_MS=12000
# 可选：结果复用与事实缓存
REPORT_REUSE_LOOKBACK_LIMIT=20
REPORT_CACHE_TTL_MS=300000
FACT_CACHE_TTL_MS=120000
TREND_BRIEF_FACT_CACHE_TTL_MS=180000
# 可选：runtime（旧版 {report} 协议）| responses（/v1/responses）| auto（按 URL 自动判断）
OPENCLAW_COMPAT_MODE=auto
USE_OPENCLAW_RUNTIME=true
OPENCLAW_FALLBACK_TO_DIRECT=true
VITE_API_BASE_URL=/api
